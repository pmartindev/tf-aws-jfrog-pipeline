version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: 2.2
  pre_build:
    commands:
      - echo "Entering build phase..."
  build:
    commands:
      - echo "Entering build phase..."
      - cd /
      - jfrog rt c rt-server-1 --url=http://13.57.10.242/artifactory --apikey AKCp5cckUWwkCP31i64s8ayR3HVs5GhxJVHdX5AYM1HCugCWSeCto7da2zV1JuB2ZYEPvtPb8
      - jfrog rt ping --server-id=rt-server-1
      - touch ./largeFile.txt
      - jfrog rt u ./largeFile.txt example-repo-local
      - terraform --version
      - dotnet --version
  post_build:
    commands:
      - echo " Entering post build phase..."

